global:
  scrape_interval: 5s # Vai coletar dados a cada 5 segundos

scrape_configs:
  - job_name: 'nestjs-monolito'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['host.docker.internal:3000'] # Aponta para o NestJS rodando no seu Windows